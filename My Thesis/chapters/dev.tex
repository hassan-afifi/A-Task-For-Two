\chapter{Developer Documentation}

\section{Detailed Problem Specification}
The implemented software is a two-player online cooperative puzzle prototype. The core technical objective is to guarantee consistent multiplayer puzzle state while preserving clear separation between networking, gameplay rules, and UI concerns.

The practical engineering challenge is not only puzzle generation, but coordinated runtime behavior across multiple subsystems:
\begin{itemize}
\item reliable host/client session initialization,
\item authority-controlled state mutation,
\item predictable user input flow during gameplay and menu overlays,
\item deterministic puzzle progression and lock logic,
\item robust end/disconnect transitions.
\end{itemize}

\newpage

\subsection*{Functional Requirements Breakdown}
The game loop is implemented through the following functional requirements.
\begin{enumerate}
\item Two participants establish an online session by host creation and join-code-based client connection.
\item Both participants load the same gameplay scene and own only their local player object.
\item Each participant receives one local equation view with partial information.
\item Each participant rotates one interactive die that contributes to the counterpart's missing value.
\item Puzzle solved state is accepted only after continuous correctness hold.
\item Solved puzzle raises a barrier and enables level completion.
\item Entering the end trigger transitions all participants to a shared end screen.
\item Host disconnect before victory transitions the connected client to a connection-lost flow.
\end{enumerate}

\begin{table}[H]
\centering
\caption{Functional Requirement to Implementation Mapping}
\label{tab:functional-mapping}
\begin{tabular}{|>{\centering\arraybackslash}m{0.1\linewidth}|>{\centering\arraybackslash}m{0.3\linewidth}|>{\centering\arraybackslash}m{0.5\linewidth}|}
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Primary Implementation Components} \\
\hline
FR-1 & Session create/join lifecycle & \texttt{MainMenu}, \texttt{RelayManager}, \texttt{GameSession} \\
\hline
FR-2 & Ownership-safe gameplay entry & \texttt{RelayManager}, \texttt{PlayerMovement}, \texttt{PlayerInputHandler} \\
\hline
FR-3 & Asymmetric local puzzle information & \texttt{PuzzleController} display data per side \\
\hline
FR-4 & Cross-participant die effect & \texttt{DieClickAnimate}, \texttt{PuzzleController} target mapping \\
\hline
FR-5 & Continuous correctness hold validation & \texttt{PuzzleController.HoldOk} and solve timer \\
\hline
FR-6 & Barrier open transition & \texttt{PuzzleController.RaiseBars} \\
\hline
FR-7 & Shared victory state & \texttt{EndScreen} (RPC-triggered local show) \\
\hline
FR-8 & Host-disconnect recovery state & \texttt{ConnectionLost} callback handling \\
\hline
\end{tabular}
\end{table}

Figure~\ref{fig:connection-lost-flow} shows the connection-lost screen presented on clients when host connection is interrupted.

\subsection*{Non-Functional Requirements}
The implementation targets the following quality constraints:
\begin{itemize}
\item deterministic puzzle resolution and server-authoritative updates,
\item stable scene/menu/session transitions without orphaned network state,
\item maintainable script boundaries with explicit setup validation,
\item persistent options and session identity data,
\item behavior-focused automated test coverage in both edit mode and play mode.
\end{itemize}

\begin{table}[H]
\centering
\caption{Non-Functional Requirement Verification}
\label{tab:nonfunctional-verification}
\begin{longtable}{|>{\centering\arraybackslash}m{0.2\linewidth}|>{\centering\arraybackslash}m{0.35\linewidth}|>{\centering\arraybackslash}m{0.35\linewidth}|}
\hline
\textbf{Requirement} & \textbf{Implementation Strategy} & \textbf{Verification Approach} \\
\hline
Deterministic puzzle state & Server-side \texttt{NetworkVariable\allowbreak<NetworkPuzzleState>} updates & Puzzle play tests (regenerate, solve, equals, lock behavior) \\
\hline
Session lifecycle stability & Centralized create/join/shutdown flow & Main/pause/network UI play tests \\
\hline
Maintainable module boundaries & Responsibility-based script grouping & Class/module review plus integration tests \\
\hline
Persistent user options & \texttt{PlayerPrefs}-backed options/session storage & Edit/play tests for saved values and bounds \\
\hline
Runtime reliability & Fail-fast setup checks and guarded transitions & Automated tests + manual multi-client runs \\
\hline
\end{longtable}
\end{table}

\newpage

\section{Concepts and Definitions}
Table~\ref{tab:concept-definitions} summarizes the main technical terms used throughout the implementation.

\begin{table}[H]
\centering
\caption{Concepts and Definitions}
\label{tab:concept-definitions}
\begin{longtable}{|>{\centering\arraybackslash}m{0.25\linewidth}|>{\centering\arraybackslash}m{0.25\linewidth}|>{\centering\arraybackslash}m{0.4\linewidth}|}
\hline
\textbf{Term} & \textbf{Definition} & \textbf{Project-Specific Usage} \\
\hline
Host & Client that also runs server authority & Creates Relay allocation and controls authoritative state mutations \\
\hline
Client & Remote participant connected to host & Joins with code and receives replicated state \\
\hline
Server-authoritative state & State committed on server then replicated & Puzzle solved flag, die face progression, lock status \cite{unity_ngo_networkvariable_docs,unity_ngo_rpc_docs,unity_ngo_ownership_docs} \\
\hline
\texttt{NetworkVariable} & Replicated container for synchronized values & Puzzle state and die lock/face synchronization \cite{unity_ngo_networkvariable_docs} \\
\hline
RPC & Networked remote call mechanism & Roll requests, regenerate requests, end-screen broadcast \cite{unity_ngo_rpc_docs} \\
\hline
Ownership & Authority assignment for a network object & Player movement/input/interaction only for local owner \cite{unity_ngo_ownership_docs} \\
\hline
\end{longtable}
\end{table}

\section{System Overview}
The application is implemented in Unity with C\# scripts, Netcode for GameObjects replication, and Relay transport integration \cite{unity_ngo_networkvariable_docs,unity_ngo_rpc_docs,unity_relay_docs}. Runtime responsibilities are intentionally split into four cooperating layers:
\begin{itemize}
\item \textbf{Session and networking layer}: menu validation, relay create/join, net startup, scene transition.
\item \textbf{Gameplay layer}: player movement/interaction, die animation, puzzle solve state, barrier/open flow.
\item \textbf{UI and settings layer}: menu panels, pause/options behavior, persisted user preferences.
\item \textbf{Verification layer}: edit mode and play mode behavior tests.
\end{itemize}

\subsection*{Runtime Lifecycle}
The runtime sequence follows a deterministic system-level flow:
\begin{enumerate}
\item Main menu validates name/code input and enables create/join actions.
\item Host allocates Relay session; client joins by code.
\item Netcode starts host/client and transitions to gameplay scene.
\item Local owner components activate input, camera, and movement.
\item Participants interact with puzzle dice through ownership-safe raycast actions.
\item Server-authoritative logic evaluates the hold condition and updates the shared puzzle state accordingly.
\item Barrier opens and end trigger broadcasts shared end-screen state.
\item Host disconnect before victory redirects the connected client to connection-lost recovery UI.
\end{enumerate}

This separation prevents network transport concerns from propagating into UI components and keeps gameplay logic independent of menu panel implementation details.

\section{Architecture Rationale}
\subsection*{Design Principles}
The architecture follows a separation-of-concerns strategy to isolate session transport, gameplay rule evaluation, and user-interface behavior. This design is necessary because host/join flow, replicated puzzle state, and menu interactions operate concurrently within the same runtime lifecycle. Clear responsibility boundaries therefore reduce cross-module coupling during iterative development.

A server-authoritative model was selected for gameplay-critical state transitions. Puzzle progression, solve validation, die lock state, and terminal flow triggers are committed through server-authoritative logic and then propagated through replicated state containers and RPC events. This avoids conflicting client-side writes and guarantees a single authorized  multiplayer state.

Lifecycle operations were intentionally centralized. Input validation, Relay initialization, transport configuration, scene transition, and shutdown handling are orchestrated by dedicated session/menu components rather than distributed across gameplay scripts. This improves operational predictability and simplifies debugging of create/join/return paths.

Fail-fast initialization checks were also adopted for components with dense serialized dependencies. Early detection of missing references is preferable to silent partial behavior during runtime, and it improves repeatability in both automated and manual validation workflows.

\subsection*{Entity-Component Architecture in Unity}
The implementation follows Unity's component-based architecture \cite{unity_components_docs}. Runtime entities are represented as \texttt{GameObject} instances, while behavior is composed through attached components (for example, \texttt{PlayerInputHandler}, \texttt{PlayerMovement}, \texttt{PlayerInteractor}, and \texttt{PlayerVisuals} on player objects).

This composition strategy was selected to keep responsibilities modular, avoid inheritance-heavy designs, and support iterative prefab-based assembly. Multiplayer-specific behavior is integrated through \texttt{NetworkBehaviour} components for replicated state and authority-aware execution, while non-networked concerns remain in regular \texttt{MonoBehaviour} components \cite{unity_monobehaviour_docs}.

The resulting entity-component style is well suited to the thesis scope: explicit runtime composition, maintainable subsystem boundaries, and direct compatibility with the selected Unity Netcode workflow.

The resulting architecture prioritizes correctness, observability, and maintainability over feature breadth. This tradeoff is appropriate for the thesis scope and provides a stable base for future extensions. The combination of component-based composition, authoritative state management, and layered module separation enables predictable runtime behavior and supports iterative development.

\section{System Modules}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/ModuleDiagram.png}
\caption{General module architecture of \textit{A Task For Two} and its Unity platform dependencies.}
\label{fig:module-diagram}
\end{figure}

\subsection*{Module Purpose and Responsibilities}
Figure~\ref{fig:module-diagram} presents a high-level module view of the system architecture, abstracting implementation details into responsibility-driven layers.

\begin{itemize}
\item \textbf{Presentation Layer}: contains \textit{Menu and Session UI}, \textit{Pause, End, and Recovery UI}, and \textit{In-Game HUD and Feedback}. It collects user actions and displays player/puzzle state.
\item \textbf{Gameplay Layer}: contains \textit{Game Flow Control}, \textit{Player Control and Interaction}, and \textit{Cooperative Puzzle Rules}. It processes interactions and enforces puzzle/gameplay behavior.
\item \textbf{Networking Layer}: contains \textit{Session Lifecycle}, \textit{State Replication and Ownership Management}, and \textit{Authoritative State Evaluation}. It validates shared-state changes and publishes canonical multiplayer state.
\item \textbf{Persistence Layer}: contains \textit{Local Preferences and Profile Cache}. It stores and restores device-local settings and identity-related values.
\item \textbf{Unity Platform and Services}: external dependencies shown in the diagram are \textit{Unity Relay}, \textit{Unity Authentication Services}, \textit{PlayerPrefs Storage}, and \textit{Netcode for GameObjects}.
\end{itemize}

\subsection*{Interaction Flow and Design Intent}
The dependency direction in Figure~\ref{fig:module-diagram} is intentional and reflects the runtime control path:
\begin{itemize}
\item \textit{Menu and Session UI} issues start-session actions to \textit{Session Lifecycle}.
\item \textit{Session Lifecycle} initializes network state via \textit{State Replication and Ownership Management}, while transport setup and service identity are delegated to \textit{Unity Relay} and \textit{Unity Authentication Services}.
\item \textit{Player Control and Interaction} sends interaction events to \textit{Cooperative Puzzle Rules}; puzzle outcomes are passed to \textit{Authoritative State Evaluation} for solve validation.
\item \textit{Authoritative State Evaluation} publishes shared state through \textit{State Replication and Ownership Management}, which then synchronizes clients through the Netcode replication API.
\item Presentation rendering is fed by gameplay state (\textit{render player state}, \textit{render puzzle state}), and gameplay end/recovery outcomes drive \textit{Pause, End, and Recovery UI} transitions.
\item \textit{Local Preferences and Profile Cache} supports load/save settings, restores session data to networking, and persists local data through \textit{PlayerPrefs Storage}.
\end{itemize}

This design ensures responsive user interaction, consistent multiplayer behavior, and explicit subsystem boundaries that support maintainability, testing, and future extension.

\subsection*{Module Responsibilities}
The following subsections describe the main runtime modules and their responsibilities within the system architecture.

\subsection*{Session and Networking Module}
This module handles menu-triggered session lifecycle from initial input to synchronized scene transition.

\textbf{Main responsibilities:}
\begin{itemize}
\item \texttt{MainMenu} validates user input (name with at least one letter, join code length of six).
\item \texttt{GameSession} stores persistent session identity values (name and selected character) and current join code.
\item \texttt{RelayManager} initializes Unity Services/authentication, allocates or joins Relay, configures transport, starts host/client, and triggers scene load \cite{unity_authentication_docs,unity_relay_docs}.
\item An ownership correction step is applied if player object authority is not assigned during initial connection (implemented in \texttt{RelayManager.EnsureOwner}).
\item \texttt{MenuActions.StopNet} centralizes network shutdown before scene return or application exit.
\end{itemize}

\textbf{Design rationale:} transport configuration and network startup remain outside gameplay scripts, so puzzle/player components are not coupled to relay allocation details.

\subsection*{Player Module}
The player module is explicitly decomposed into input capture, movement simulation, world interaction, visual replication, and movement audio.

\newpage

\textbf{Input and movement details:}
\begin{itemize}
\item \texttt{PlayerInputHandler} captures action-map state and gates input by pause/disconnect state.
\item \texttt{PlayerMovement} applies first-person movement and camera look using local-owner execution.
\item Spawn positions are role-based (host side and client side are separated).
\item Movement parameters include walk speed, sprint multiplier, crouch speed multiplier, gravity scaling, jump power, and cooldown.
\end{itemize}

\textbf{Interaction and presentation details:}
\begin{itemize}
\item \texttt{PlayerInteractor} performs center-screen raycast interaction with a fixed max distance.
\item \texttt{PlayerVisuals} replicates selected character and player name; local body mesh is hidden from owner view via layer assignment.
\item \texttt{PlayerAudio} synchronizes movement loop and landing cues to non-owner peers through targeted RPC dispatch.
\item \texttt{CharacterAnimation} maps movement state to animator parameters and jump trigger.
\end{itemize}

\subsection*{Puzzle Module}
Puzzle behavior is implemented by \texttt{PuzzleController} and \texttt{DieClickAnimate}.

\textbf{Puzzle state model:}
\begin{itemize}
\item Two local displays hold equation data: missing value, two known values, and sum.
\item The authoritative puzzle state is encapsulated within \texttt{NetworkPuzzleState} and distributed through \texttt{NetworkVariable}.
\item Target mapping is cross-linked: die 1 solves equation 2 missing value and die 2 solves equation 1 missing value.
\end{itemize}

\newpage

\textbf{Generation and solve algorithm:}
\begin{itemize}
\item Symbol values are generated in the \([0,9]\) range.
\item Equation-pair generation explicitly rejects the both-missing-zero case.
\item Solve acceptance requires both dice to be correct and not rolling for a continuous hold duration.
\item Any wrong value or ongoing roll immediately resets hold progress.
\item On solve commit, both dice are locked and barrier animation starts.
\end{itemize}

\textbf{Timing constants in implementation:}
\begin{itemize}
\item die roll phase duration: \texttt{0.28s},
\item solve hold requirement: \texttt{0.82s},
\item barrier raise duration: \texttt{1.28s},
\item barrier raise offset: \texttt{4.82} local units.
\end{itemize}

\subsection*{UI and Options Module}
The options subsystem applies changes at runtime and stores them in local persistent storage.

\subsubsection*{Display and Camera Settings}
\begin{table}[H]
\centering
\caption{Display and Camera Options}
\label{tab:display-camera-options}
\begin{tabular}{|>{\centering\arraybackslash}m{0.2\linewidth}|>{\centering\arraybackslash}m{0.25\linewidth}|>{\centering\arraybackslash}m{0.45\linewidth}|}
\hline
\textbf{Setting} & \textbf{Range / Modes} & \textbf{Runtime Behavior} \\
\hline
Display mode & Windowed / Borderless / Fullscreen & Applies fullscreen mode immediately, borderless uses highest-ranked available resolution. \\
\hline
Resolution & Available monitor resolutions & In fullscreen, list is filtered by aspect ratio, in borderless, manual selection is disabled. \\
\hline
Quality level & High / Medium / Low & Maps the selected option to the corresponding \texttt{QualitySettings} preset index and applies it at runtime. \\
\hline
Field of view & 60--100 (default 80) & Applies to owner camera and persists. \\
\hline
Sensitivity & 1--100\% (step 1) & Mapped to runtime sensitivity 0.01--1.00 and applied to owner movement. \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Audio Settings}
\begin{table}[H]
\centering
\caption{Audio Options}
\label{tab:audio-options}
\begin{tabular}{|>{\centering\arraybackslash}m{0.25\linewidth}|>{\centering\arraybackslash}m{0.1\linewidth}|>{\centering\arraybackslash}m{0.55\linewidth}|}
\hline
\textbf{Setting} & \textbf{Range} & \textbf{Runtime Behavior} \\
\hline
Master volume & 0--100\% & Controls master mixer parameter (fallback to listener when needed). \\
\hline
Game SFX volume & 0--100\% & Controls gameplay sound effects channel. \\
\hline
Menu SFX volume & 0--100\% & Controls UI hover/click sound channel. \\
\hline
Game music volume & 0--100\% & Controls gameplay music channel; can be muted by end/disconnect flow. \\
\hline
Menu music volume & 0--100\% & Controls menu music channel. \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Crosshair and HUD Settings}
\begin{table}[H]
\centering
\caption{Crosshair and HUD Options}
\label{tab:hud-options}
\begin{tabular}{|>{\centering\arraybackslash}m{0.25\linewidth}|>{\centering\arraybackslash}m{0.2\linewidth}|>{\centering\arraybackslash}m{0.45\linewidth}|}
\hline
\textbf{Setting} & \textbf{Range / States} & \textbf{Runtime Behavior} \\
\hline
Crosshair size & 1--10 & Mapped to 5--50 pixel UI size. \\
\hline
Crosshair color & 10 preset colors & Applies immediately to crosshair graphic. \\
\hline
Show FPS & On / Off & Toggles FPS widget visibility. \\
\hline
Show Ping & On / Off & Toggles ping widget visibility. \\
\hline
Show System Clock & On / Off & Toggles clock widget visibility. \\
\hline
\end{tabular}
\end{table}

All options are persisted through \texttt{PlayerPrefs} \cite{unity_playerprefs_docs} and reloaded in both main-menu and pause-menu contexts.

\subsection*{End and Recovery Module}
End-of-session behavior is split into success and recovery paths.
\begin{itemize}
\item \texttt{EndScreen} watches trigger occupancy, broadcasts shared end-state, disables owner controls, and performs a timed fade sequence before showing end UI.
\item \texttt{ConnectionLost} listens for local disconnect callbacks on the connected client (excluding intentional shutdown and server-side paths) and shows recovery UI when host connection is lost.
\item Both flows call \texttt{PauseMenu.ForceClose()} to avoid conflicting overlays.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/PauseMenu.png}
\caption{Pause menu overlay during active gameplay runtime.}
\label{fig:pause-overlay}
\end{figure}

\section{Data Structures and State Management}
The project does not rely on an external relational database. This choice aligns with the scope and requirements of a small-scale multiplayer prototype. State is split into replicated runtime state and local persisted configuration.

\subsection*{Runtime and Networked State}
\begin{table}[H]
\centering
\caption{Primary Runtime State Containers}
\label{tab:runtime-state-containers}
\begin{longtable}{|>{\centering\arraybackslash}m{0.22\linewidth}|>{\centering\arraybackslash}m{0.3\linewidth}|>{\centering\arraybackslash}m{0.38\linewidth}|}
\hline
\textbf{Container} & \textbf{Type} & \textbf{Purpose} \\
\hline
\texttt{netState} in \texttt{PuzzleController} & \texttt{NetworkVariable\allowbreak<NetworkPuzzleState>} & Replicates both equations, targets, solved flag, and version. \\
\hline
\texttt{netFace} in \texttt{DieClickAnimate} & \texttt{NetworkVariable<int>} & Replicates current face index for each die. \\
\hline
\texttt{netLocked} in \texttt{DieClickAnimate} & \texttt{NetworkVariable<bool>} & Replicates roll lock state. \\
\hline
\texttt{netChar} in \texttt{PlayerVisuals} & \texttt{NetworkVariable<int>} & Replicates selected character index. \\
\hline
\texttt{netName} in \texttt{PlayerVisuals} & \texttt{NetworkVariable\allowbreak<FixedString64Bytes>} & Replicates normalized player display name. \\
\hline
\end{longtable}
\end{table}

\subsection*{Persistent Local State}
Persistent values are stored with \texttt{PlayerPrefs} \cite{unity_playerprefs_docs}. Table~\ref{tab:prefs-keys} lists the active keys.

\begin{table}[H]
\centering
\caption{Persistent Preference Keys}
\label{tab:prefs-keys}
\begin{tabular}{|>{\centering\arraybackslash}m{0.4\linewidth}|>{\centering\arraybackslash}m{0.08\linewidth}|>{\centering\arraybackslash}m{0.42\linewidth}|}
\hline
\textbf{Key} & \textbf{Type} & \textbf{Meaning} \\
\hline
\texttt{session\_player\_name} & string & Last entered player name. \\
\hline
\texttt{session\_char\_index} & int & Last selected character index. \\
\hline
\texttt{opt\_display\_mode} & int & Display mode option index. \\
\hline
\texttt{opt\_resolution\_width} / \texttt{opt\_resolution\_height} & int & Selected resolution dimensions. \\
\hline
\texttt{opt\_quality\_level} & int & Quality option index. \\
\hline
\texttt{opt\_camera\_fov} & float & Owner camera field-of-view. \\
\hline
\texttt{opt\_camera\_sensitivity\_pct} & float & Mouse sensitivity percentage. \\
\hline
\texttt{opt\_master\_volume} & float & Master volume percentage. \\
\hline
\texttt{opt\_game\_sfx\_volume} & float & Gameplay SFX volume percentage. \\
\hline
\texttt{opt\_menu\_sfx\_volume} & float & Menu SFX volume percentage. \\
\hline
\texttt{opt\_game\_music\_volume} & float & Gameplay music volume percentage. \\
\hline
\texttt{opt\_menu\_music\_volume} & float & Menu music volume percentage. \\
\hline
\texttt{opt\_crosshair\_size} & float & Crosshair size slider value. \\
\hline
\texttt{opt\_crosshair\_color} & int & Crosshair color preset index. \\
\hline
\texttt{opt\_show\_fps} / \texttt{opt\_show\_ping} / \texttt{opt\_show\_system\_clock} & int & HUD widget visibility flags. \\
\hline
\end{tabular}
\end{table}

The boundary is explicit: multiplayer state is networked, while configuration remains local.

\section{Objects Structure}
The project is partitioned into script groups by responsibility:
\begin{itemize}
\item \textbf{Session/flow}: \texttt{MainMenu}, \texttt{RelayManager}, \texttt{GameSession}, \texttt{MenuActions}, \texttt{PauseMenu}, \texttt{ConnectionLost}, \texttt{EndScreen}.
\item \textbf{Player control/presentation}: \texttt{PlayerInputHandler}, \texttt{PlayerMovement}, \texttt{PlayerInteractor}, \texttt{PlayerVisuals}, \texttt{PlayerAudio}, \texttt{CharacterAnimation}, \texttt{CharacterSelection}.
\item \textbf{Puzzle domain}: \texttt{PuzzleController}, \texttt{DieClickAnimate}.
\item \textbf{UI widgets}: \texttt{OptionsMenu}, \texttt{SliderHelper}, \texttt{AnimatedToggle}, \texttt{GenderToggle}, \texttt{DropdownHelper}, \texttt{HudStats}, \texttt{MenuSfx}.
\item \textbf{Service}: \texttt{AudioManager}.
\end{itemize}

Two design rules are applied consistently:
\begin{itemize}
\item \textbf{Authority rule}: gameplay mutations that affect shared state are validated and committed through server-authoritative logic.
\item \textbf{Fail-fast setup rule}: major runtime components validate serialized references during startup and throw explicit setup errors when required references are missing.
\end{itemize}

This approach reduces hidden coupling and improves runtime observability and diagnosability.

\section{Physical Structure}
The repository follows Unity conventions with explicit test separation.
\begin{itemize}
\item \texttt{Assets/Animations}: animation clips and animator-related assets.
\item \texttt{Assets/Audio}: audio clips, mixer assets, and sound resources.
\item \texttt{Assets/Materials}: rendering materials used by scene and prefab assets.
\item \texttt{Assets/Models}: imported 3D model assets.
\item \texttt{Assets/Prefabs}: reusable scene objects for players, puzzle, and UI.
\item \texttt{Assets/Scenes}: menu and gameplay scenes.
\item \texttt{Assets/Scripts}: gameplay, UI, networking, and service logic.
\item \texttt{Assets/Settings}: project-level render and runtime settings assets.
\item \texttt{Assets/Tests/EditMode}: fast logic/component tests.
\item \texttt{Assets/Tests/PlayMode}: runtime and integration-like behavior tests.
\item \texttt{Assets/TextMeshPro}: TextMeshPro fonts and TMP configuration assets.
\item \texttt{Assets/Textures}: texture resources used by models and UI elements.
\item \texttt{Packages}: Unity package dependencies and package configuration.
\item \texttt{ProjectSettings}: rendering, input, quality, and engine settings.
\end{itemize}

This structure supports clear navigation for development and reproducible builds.

\section{Development Setup}
\subsection*{Development Requirements}
For development, testing, and source-level debugging, the following tools were used:
\begin{itemize}
\item Unity Hub,
\item Unity Editor \texttt{6000.3.10f1},
\item Git,
\item Git LFS,
\item Visual Studio Code.
\end{itemize}

\subsection*{Repository Setup and Editor Startup}
\begin{enumerate}
\item Clone the repository:
\begin{verbatim}
git clone https://github.com/hassan-afifi/A_Task_For_Two.git
\end{verbatim}
or
\begin{verbatim}
git clone git@github.com:hassan-afifi/A_Task_For_Two.git
\end{verbatim}

\item Enter the project directory:
\begin{verbatim}
cd A_Task_For_Two
\end{verbatim}

\item Pull large assets managed by Git LFS:
\begin{verbatim}
git lfs install
git lfs pull
\end{verbatim}

\item Open Unity Hub, select \textbf{Add project from disk}, choose the repository folder, and open it with Unity Editor \texttt{6000.3.10f1}.
\item Open the \texttt{MainMenu} scene and press Play to verify that menu panels, name input, and options UI initialize correctly.
\end{enumerate}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/UnityProject.png}
\caption{Project opened from source in Unity Editor.}
\label{fig:unity-project-open-dev}
\end{figure}

\section{Design Decisions}
The implementation incorporates several design decisions derived during development and testing.

\subsection*{Continuous Solve Validation}
A key gameplay correction was enforcing continuous correctness during the solve hold duration. The final logic resets hold progress immediately when values become wrong or dice are still rolling, preventing delayed solve acceptance after transient correctness.

\subsection*{Network Lifecycle Cleanup}
A duplicate \texttt{NetworkManager} issue after returning to menu was addressed by centralizing network shutdown and object destruction in \texttt{MenuActions.StopNet}. This keeps menu return/exit paths consistent.

\subsection*{Session Initialization Order}
\texttt{GameSession} uses explicit early execution order to reduce startup race conditions with menu components that read session data during enable/awake phases.

\subsection*{UI Input Module Consistency in Tests}
play mode tests explicitly ensure EventSystem compatibility with the active Input System setup to avoid legacy module mismatches during automated UI interaction checks.

\section{Error Handling}
Error handling is implemented through explicit guard logic across startup, session actions, and shutdown paths.

\subsection*{Startup and Setup Validation}
Core runtime components apply fail-fast setup validation for required serialized references. Missing critical references are surfaced immediately during initialization to prevent partial execution with undefined behavior. This approach improves fault localization and reduces debugging time during both development and testing.

\subsection*{Session Input Guards}
Session operations are protected by deterministic menu-side validation before network calls are issued. Name and join-code constraints are validated locally, and create/join actions remain disabled until input is valid. These guards reduce invalid create/join attempts and keep session lifecycle behavior predictable.

\subsection*{Safe Shutdown and Return Flow}
Menu return and exit operations use centralized shutdown through \texttt{MenuActions.StopNet}. Network manager teardown and related cleanup are applied before scene changes, which avoids duplicate manager instances and stale network state across repeated session cycles.

\newpage

\section{Fault Recovery}
Recovery behavior covers runtime interruption scenarios and their verification paths.

\subsection*{Disconnect Recovery Flow}
Unexpected host-session interruption is handled through the dedicated \texttt{ConnectionLost} path. Client-side disconnect callbacks trigger controlled recovery UI instead of allowing gameplay to continue in a degraded network state. This keeps failure behavior explicit and prevents ambiguous user-facing outcomes.

\subsection*{Recovery Path Verification}
Recovery behavior is verified through both automated and manual validation. play mode tests cover menu/session transitions, disconnect-related flows, and runtime cleanup-sensitive paths, while edit mode checks cover local guard logic and utility behavior. Manual host/client runs further confirm disconnect recovery and return-to-menu reliability under practical conditions.

\section{Class Diagram}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/ClassDiagram.png}
\caption{A Task For Two class diagram (MVC-oriented view).}
\label{fig:class-diagram}
\end{figure}

\subsection*{Class Diagram Description}
Figure~\ref{fig:class-diagram} presents an MVC-oriented architectural projection with five groups: \textit{Model}, \textit{Controller}, \textit{View}, \textit{UI Components}, and \textit{Service Layer}. Public members are denoted by \texttt{+} and private members by \texttt{-}.

\begin{itemize}
\item \textbf{Model}: stores persistent/session state and puzzle state containers (\texttt{GameSession}, \texttt{PuzzleController}, \texttt{DieClickAnimate}).
\item \textbf{Controller}: drives user actions, session flow, and owner-side gameplay control (menu, relay, input, movement, interaction).
\item \textbf{View}: presents user-facing state and feedback, including character selection/visuals, animation/audio feedback, HUD stats, and terminal overlays.
\item \textbf{UI Components}: reusable, composable menu widgets and interaction helpers.
\item \textbf{Service Layer}: shared audio behavior through \texttt{AudioManager}.
\end{itemize}

The dependency direction is intentionally layered: high-level flow controllers orchestrate state transitions; views consume state; reusable services provide cross-cutting behavior.

\section{Testing Methodology}
Testing is implemented with Unity Test Framework \cite{unity_test_framework_docs} in two execution modes.

\subsection*{Test Design Policy}
The applied test design policy is:
\begin{itemize}
\item one focused test method per targeted public behavior,
\item behavior and branch verification over object-existence-only checks,
\item explicit naming convention for test readability,
\item setup/teardown isolation to prevent cross-test state pollution.
\end{itemize}

\subsection*{Edit Mode vs Play Mode Split}
\begin{itemize}
\item \textbf{Edit mode} is used for fast validation of isolated logic (input consumption, option mapping/clamping, persistence, static utility behavior, UI helper behavior).
\item \textbf{Play mode} is used for runtime orchestration, coroutine timing, scene transitions, network-aware components, and owner/non-owner behavior.
\end{itemize}

\subsection*{Isolation Techniques}
To preserve repeatability, tests use explicit cleanup and deterministic scaffolding:
\begin{itemize}
\item \texttt{PlayerPrefs.DeleteAll()} in setup/teardown for persistence isolation,
\item object cleanup helpers to destroy transient game objects and components,
\item network manager cleanup between tests,
\item event-system setup compatible with Input System UI behavior \cite{unity_input_system_docs}.
\end{itemize}

\subsection*{Manual Testing Method}
Manual testing complements automated tests in scenarios where real-time multiplayer timing, role asymmetry, and user coordination are central to correctness.

\textbf{Execution protocol:}
\begin{itemize}
\item Start separate host and client runtime instances from a clean menu state.
\item Execute a fixed scenario checklist from session creation to terminal flow.
\item Verify outcomes on both host and client views, not only on the local initiating side.
\item Include negative path runs (invalid input, forced disconnect, menu return and re-entry).
\item Repeat key scenarios after returning to main menu to confirm lifecycle cleanup.
\end{itemize}

\begin{table}[H]
\centering
\caption{Manual Test Execution Criteria}
\label{tab:manual-test-criteria}
\begin{longtable}{|>{\centering\arraybackslash}m{0.3\linewidth}|>{\centering\arraybackslash}m{0.6\linewidth}|}
\hline
\textbf{Criterion} & \textbf{Pass Condition} \\
\hline
Session establishment & Client joins host session successfully and both players load into gameplay. \\
\hline
Ownership and control & Each player can control only their own character and cannot control the remote one. \\
\hline
Puzzle synchronization & Die state, solve state, and barrier transition remain consistent on both clients. \\
\hline
UI transition correctness & Pause/end/disconnect overlays appear only in valid states and block gameplay input when required. \\
\hline
Recovery behavior & Disconnect and return-to-menu flows complete without stale network state in the next run. \\
\hline
\end{longtable}
\end{table}

\section{Test Results}
At the current development snapshot, the automated suite contains \textbf{90 tests}: \textbf{30 edit mode} and \textbf{60 play mode}.

\begin{table}[H]
\centering
\caption{Automated Test Files and Focus Areas}
\label{tab:test-files}
\begin{longtable}{|>{\centering\arraybackslash}m{0.35\linewidth}|>{\centering\arraybackslash}m{0.1\linewidth}|>{\centering\arraybackslash}m{0.1\linewidth}|>{\centering\arraybackslash}m{0.35\linewidth}|}
\hline
\textbf{Test file} & \textbf{Mode} & \textbf{Count} & \textbf{Primary focus} \\
\hline
\texttt{InputControlsEditTests} & Edit & 10 & Input consumption, animated toggles, slider step/hold helpers \\
\hline
\texttt{SessionServiceEditTests} & Edit & 10 & Session persistence, relay guard paths, static option mappings, audio manager \\
\hline
\texttt{UiControlEditTests} & Edit & 10 & Menu exit controls, character selection, slider hold start/stop behavior \\
\hline
\texttt{MainPausePlayTests} & Play & 10 & Main/pause menu runtime flow, copy code, force close, return to menu \\
\hline
\texttt{NetworkUiPlayTests} & Play & 10 & Relay calls, menu SFX interactions, drop hover, network UI transitions \\
\hline
\texttt{OptionsSettingsPlayTests} & Play & 10 & Option persistence bounds and audio setting behavior \\
\hline
\texttt{OptionsVisualPlayTests} & Play & 10 & Display/resolution/quality/\allowbreak crosshair/HUD visual settings \\
\hline
\texttt{PlayerPlayTests} & Play & 10 & Player spawn/despawn behavior, visuals/audio sync, FOV persistence \\
\hline
\texttt{PuzzlePlayTests} & Play & 10 & Die rolling/lock logic, puzzle regeneration, solve-related structures, end flow \\
\hline
\end{longtable}
\end{table}

\newpage

\subsection*{Manual Multi-Client Validation}
In addition to automated tests, manual host/client runs were executed to validate practical behavior under real interaction conditions, following the criteria defined in Table~\ref{tab:manual-test-criteria}. The key executed scenarios are summarized in Table~\ref{tab:manual-validation}.

\begin{table}[H]
\centering
\caption{Manual Multi-Client Validation Scenarios}
\label{tab:manual-validation}
\begin{longtable}{|>{\centering\arraybackslash}m{0.2\linewidth}|>{\centering\arraybackslash}m{0.35\linewidth}|>{\centering\arraybackslash}m{0.35\linewidth}|}
\hline
\textbf{Scenario} & \textbf{Expected Outcome} & \textbf{Observed Result} \\
\hline
Host creation and client join & Shared scene load and player ownership separation & Passed in repeated runs \\
\hline
Asymmetric puzzle communication cycle & Correct cross-value coordination required for solve & Passed, communication-dependent progression observed \\
\hline
Solve hold validation & Incorrect/rolling state resets hold progress & Passed, solve triggered only after continuous correctness \\
\hline
Pause/options during gameplay & Gameplay input blocked while menu is open & Passed, resume restores control correctly \\
\hline
Victory transition & End screen shown to all participants & Passed, owner controls disabled and fade sequence executed \\
\hline
Host disconnect before victory & Connection-lost screen shown on client & Passed, dedicated recovery flow triggered \\
\hline
\end{longtable}
\end{table}

Overall, the verification results confirm that the implemented architecture is stable for the targeted two-player scope and that critical runtime branches are covered by repeatable automated and manual checks.
